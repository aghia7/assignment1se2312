package models;

public class Point {
    private double x;
    private double y;

    // create a constructor having both parameters
    public Point(double x, double y) {
        // do smth with parameters
    }

    // getters and setters

    // method for calculating the distance from current point to destination point
    public double getDistance(Point dest) {
        return 0;
    }

    // toString()
}
